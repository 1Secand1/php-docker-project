version: '3.8'
services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/var/www/html
    ports:
      - 80 # Проброс только внутреннего порта контейнера
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
